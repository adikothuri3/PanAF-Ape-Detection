# Google Colab variant of configs/base.yaml.
#
# Differences from the baseline:
#   * device is pinned to cuda, since a Colab GPU runtime is assumed
#     (Runtime -> Change runtime type -> T4 GPU). Note that PyTorch-Wildlife
#     ignores the device argument; the adapter forces and verifies it.
#   * the full sample runs at every frame -- a T4 gets through 10 clips in
#     roughly 10 minutes, so there is no reason to subsample and end up
#     comparing a partial run against a full one.
#
# Paths are still repository-relative. If you keep the dataset on Google Drive,
# point paths.raw_data_dir at the mounted location, e.g.
#   raw_data_dir: /content/drive/MyDrive/panaf/raw

project:
  name: panaf-ape-detection
  seed: 42
  experiment_name: phase1-colab

paths:
  raw_data_dir: data/raw
  interim_data_dir: data/interim
  processed_data_dir: data/processed
  artifacts_dir: artifacts

data:
  manifest_path: data/sample_manifest.csv
  max_clips: 10
  frame_stride: 1

model:
  framework: pytorch-wildlife
  model_name: MegaDetectorV6
  variant: MDV6-yolov9-c
  confidence_threshold: 0.2
  # Colab T4. Set the runtime to GPU *before* running anything: Runtime ->
  # Change runtime type -> T4 GPU. Inference never runs on CPU (see base.yaml),
  # so this fails fast on a CPU runtime rather than running for hours.
  device: cuda

tracking:
  enabled: true
  backend: bytetrack
  minimum_track_length: 5

video:
  output_fps: 24.0
  codec: mp4v
  draw_confidence: true
  draw_track_id: true
  draw_behavior_label: true

logging:
  level: INFO
  save_run_metadata: true
